services:
  analisis-cuentas-clinicas:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./storage:/app/storage
      - ./output:/app/output
    env_file:
      - .env
    restart: unless-stopped
